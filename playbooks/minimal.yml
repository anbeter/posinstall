---
- name: Fedora/Ubuntu Minimal
  hosts: localhost
  become: true

  pre_tasks:

    - name: Carregar variáveis da distribuição
      ansible.builtin.include_vars:
        file: "{{ playbook_dir }}/vars/{{ ansible_facts['distribution'] }}.yml"
  roles:

    - repositories
    - common
    - git
