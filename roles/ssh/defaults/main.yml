---
ssh_package: openssh-clients

ssh_user: "{{ ansible_facts.env.SUDO_USER | default(ansible_facts['user_id'], true) }}"

ssh_directory: "/home/{{ ssh_user }}/.ssh"

ssh_key_type: ed25519

ssh_key_file: "{{ ssh_directory }}/id_ed25519"

ssh_config_file: "{{ ssh_directory }}/config"
