---
docker_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin

docker_service: docker

docker_users:
  - "{{ ansible_facts.env.SUDO_USER | default(ansible_facts['user_id'], true) }}"
